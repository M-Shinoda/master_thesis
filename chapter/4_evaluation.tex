\chapter{評価}

本章では，第3章で提案・実装した災害分析基盤を約9ヶ月間（2025年2月中旬〜10月）運用し，その有効性を検証した結果を述べる．
まず運用実績とシステムの安定性を示し，次に収集データの統計的特性を分析する．
最後に，地震発生時のコメント反応や感情分析結果を通じて，実際に定性的なYouTubeライブ配信コメントを定量的なデータとして扱いどのような特徴があるかを明らかにする．
\section{運用実績とシステム検証}

\subsection{データ収集実績}

2025年2月15日〜10月31日の約8.5ヶ月間で，蓄積したデータについて評価する．
\tabref{tab:key_fields_youtube_live_comments}に示したsnippet\_typeフィールドの種類別に集計した結果を\tabref{tab:data_collection_results}に示す．

\input{table/data_collection_results}

本来取得が可能であった全体のコメントの数を把握できないため，以下にシステム運用中に見つかったエラーやデータ欠損の要因をし，収集したデータ全体からコメントデータが十分に確保できているかを検証した．

まず，運用中の2025年7月6日早朝から半日程度，検証用の別システムの影響により，システムが停止しデータが欠損ていたことが確認された．

また，収集対象のライブ配信では地震規模が一定レベルを超えた場合，書込み制限が起こるため，システムの停止以外に最低でも5分程度欠損が起こる場合が確認された．
その上で，YouTubeでは，1日にリクエストできる上限と，1度のリクエスト含まれるコメントの上限は2000個である．
しかし，実際に一度のリクエストに含まれたコメントは最高で397件であったため，システムの稼働中はデータが確実に収集できていると考えられる．

収集した全483742件のデータのうち，textMessageEventが大半を占めており，分析対象となるコメントデータは全体の約98\%を占めている．
システムの停止期間は全体に比べて小さくコメントデータは十分に確保できていると考えられる．

\subsection{可視化}
\ref{sec:visualization_layer}で可視化した例として，\figref{fig:bert_emotion_count_earthquake}や\figref{fig:web_dashboard}を示した．
特に\figref{fig:bert_emotion_count_earthquake}では2025年6月中かつ，BERTベースの感情分析を可視化している．
これに追加して，2025年6月中のLUKEベースの感情分析と2025年7月中のBERT，LUKEベースの感情分析も同様にLooker Studioで可視化したものを\figref{fig:bert_emotion_count}，\figref{fig:luke_wrime_emotion_count}，\figref{fig:luke_wrime_emotion_count_earthquake}に示す．また，2025年7月中をWebアプリケーションのダッシュボード画面の例を\figref{fig:web_dashboard_2}に示す．

\input{figure/bert_emotion_count}
\input{figure/luke_wrime_emotion_count_earthquake}

\input{figure/web_dashboard_2}

\clearpage

2025年6月中(\figref{fig:web_dashboard})と2025年7月中(\figref{fig:web_dashboard_2})のデータは，最も多くの地震情報があった期間であり，ダッシュボード上で地震発生時のコメント数の急増や感情分析結果の変化を確認できる．
6月中であれば地震情報の件数にライブイベントの件数が追従している様子が見られた．
可視化したことにより，イベント件数と地震の関係を直感的に把握できるようになったと言える．

しかし，7月中では地震情報の件数に対してライブイベントの件数が追従していない様子が中盤でよく見られた．
一般的に，人が少ない地域での地震や，人が感じ辛い震度の地震の場合，地震情報とコメント量が比例しない場合がある．
しかし，7月中では地震情報のほかにも通常とは異なる状態が発生しており，それが観測結果に影響を与えた可能性がある．具体的には，2025年7月5日を中心とした憶測やデマ情報がSNSや動画配信を通じて拡散しており，こうした情報環境の変化が，利用者の一時的なトレンドや災害関連コメントの増加を促した可能性が示唆される\cite{NHK2025k10014855191000}．
そのため，これらに該当する期間を切り分けた分析が必要である．
以降の分析では7月1日〜8日のデータを含む分析と除外した分析を行う．

%TODO: 
% \subsection{API利用状況とQuota検証}

% 実際の運用で以下を確認した．

% \begin{itemize}
%     \item \textbf{Quota消費量の実測}: GCPダッシュボードで確認した結果，1リクエストあたり5 Quotaを消費することを実証．公式ドキュメントに記載がないため，今後の仕様変更に注意が必要．
%     \item \textbf{2分間隔の妥当性}: 9ヶ月間の運用で1日のQuota上限（10,000）に到達せず，平均消費量は約3,600 Quota/日（720リクエスト）であった．
%     \item \textbf{取得漏れの検証}: 地震発生後の高頻度投稿時も，2分間隔で十分にコメントを捕捉できることを確認．
% \end{itemize}


\section{収集したデータの分析}

\subsection{コメント文字数の分布}
YouTubeライブコメントは最大200文字という制約があり，実際の分布は以下で説明する．
\tabref{tab:comment_length_statistics}にコメント文字数の統計量を示す．
また，\figref{fig:comment_length_boxplot}に箱ひげ図，\figref{fig:comment_length_distribution_linear}に線形スケール，\figref{fig:comment_length_distribution_log}に対数スケールでの分布を示す．

\input{table/comment_length_statistics}

\clearpage

\input{figure/comment_length_boxplot}

\clearpage

\input{figure/comment_length_distribution_linear}
\input{figure/comment_length_distribution_log}

\clearpage
本データ（N = 255122）における YouTube ライブコメントの文字数分布は，短文コメントが圧倒的多数を占める一方で，少数の長文コメントが裾野を形成する強い右裾分布示している．

具体的に示すと，平均約15文字，中央値でも10文字という結果であった．
Xでは地震に限られない抽象的災害ツイートの平均文字数が半角121文字とされている\cite{Garg2025BD2TSumm}．
そのため，YouTubeライブコメントはXと比べてもかなり短い傾向があることがわかった．

箱ひげ図では，四分位範囲を超える外れ値を除外した場合でも，上側のひげは 36 文字付近まで伸びており，短文が主流である一方で，一定量のやや長めのコメントが安定して存在することが確認できる．
なお，本分析では15805件の外れ値，つまり長文コメントが存在しており，これらは分布の可視化上は除外されているが，特徴の一つと言える．

線形スケールのヒストグラムでは，5〜8文字付近に明確なピークが存在し，その後，文字数の増加に伴ってコメント数が急速に減少している．
30文字を超えるコメントは急激に少なくなり，50文字以上のコメントは全体の中ではごく限られた割合である．

対数スケールのヒストグラムでは，線形スケールでは視認しにくかった 長文コメントの裾野構造が明瞭に確認できる．
コメント数は文字数の増加に対してほぼ単調減少しているものの，100〜200 文字程度のコメントも継続的に存在しており，完全に無視できる例外ではないことが分かる．

また，0文字でのコメントも一定数存在しており，これらは空白コメントであることが確認された．


\subsection{コメント数の分布}

コメント数の時間的分布を，時間帯別，曜日別，月別に集計し，地震発生件数との関係を分析した結果を\figref{fig:comment_count_by_hour}，\figref{fig:comment_count_by_day_of_week}，\figref{fig:comment_count_by_month}に示す．

\input{figure/comment_count_by_hour}
\input{figure/comment_count_by_day_of_week}
\input{figure/comment_count_by_month}

\subsubsection{時間帯別分布}
\figref{fig:comment_count_by_hour}に示すように，1日24時間におけるコメント数は時間帯によって大きく変動している．
最もコメント数が少ない時間帯は5〜7時の早朝帯であり，約5,000〜6,000件程度である．
一方，最もコメント数が多い時間帯は23時で約30,000件に達し，深夜帯の約6倍のコメント数を記録している．
19時から23時にかけての夜間帯は，指数関数的にコメント数が上昇する傾向が見られ，特に21時から23時にかけては急激な増加が確認できる．
地震発生件数は12時台に約141件で最大値を示し，3時台に約77件で最小値を示している．
全体的に，コメント数の時間帯別変動パターンと地震発生件数の時間帯別パターンには明確な相関関係は見られない．

\subsubsection{曜日別分布}
\figref{fig:comment_count_by_day_of_week}に示すように，曜日別のコメント数は約26,000件〜約47,000件の範囲で変動している．
最もコメント数が多いのは水曜日で約47,000件，次いで月曜日が約43,000件である．
最もコメント数が少ないのは木曜日で約29,000件である．
地震発生件数は月曜日に約460件で最大，木曜日に約260件で最小を記録している．
月曜日はコメント数と地震発生件数がともに高い値を示しているが，水曜日はコメント数が最大であるにもかかわらず地震発生件数は約365件と中程度である．
曜日間でのコメント数の変動幅は約1.8倍であり，時間帯別や月別と比較して変動は小さい．

\subsubsection{月別分布}
\figref{fig:comment_count_by_month}に示すように，2025年2月から10月までの月別コメント数は約10,500件〜約51,000件の範囲で大きく変動している．
最もコメント数が多いのは6月で約51,000件，次いで7月が約51,000件である．
コメント数が少ないのは2月で約10,500件である．
地震発生件数は6月に約740件で最大，2月に約80件で最小を記録している．
しかし，2月は収集期間が約半月であるため，コメント数と地震発生件数が確実に最小とは言えない．
6月と7月はコメント数と地震発生件数がともに突出して高い値を示している．
5月から7月にかけて，コメント数と地震発生件数がともに急増している傾向が確認できる．

\subsection{ユーザごとのコメント数分布}

ユーザ別のコメント投稿パターンを分析した結果を\figref{fig:user_comment_distribution}，\figref{fig:top_30_users_comment_count}，\figref{fig:activity_period_comment_heatmap}に示す．

\input{figure/user_comment_distribution}
\input{figure/top_30_users_comment_count}
\input{figure/activity_period_comment_heatmap}

\subsubsection{ユーザごとのメッセージ数分布}
\figref{fig:user_comment_distribution}に示すように，ユーザ母数35,228人によって投稿された255,122件のコメントは，ユーザ間で極めて偏った分布を示している．
メッセージ数が1〜10件のユーザが対数スケールで$10^4$オーダー（約30,000人以上）存在しており，全ユーザの大部分を占めている．
一方，メッセージ数が100件を超えるユーザは急激に減少し，$10^2$オーダー（約100人程度）となる．
200件を超えるユーザは$10^1$オーダー（約10人程度）まで減少し，400件以上のユーザは数人程度である．
特に，700件付近に1つの顕著なピークと偏りが確認でき，これは特定の高頻度投稿ユーザのグループが存在していることを示している．
全体として，少数の投稿回数が非常に多いユーザと，圧倒的多数の投稿回数が少ないユーザという二極化した構造が明確である．

\subsubsection{上位30ユーザのコメント数}
\figref{fig:top_30_users_comment_count}に示すように，上位30ユーザのコメント数は約600件から約2,300件の範囲に分布している．
最もコメント数が多いのは公式アカウントで約2,300件である．
この公式アカウントとはライブ配信の主催者が運営するアカウントであり，地震情報の提供や視聴者との交流を目的としている．
ユーザ2位は約2,200件，ユーザ3位は約2,100件と，上位3ユーザはいずれも2,000件を超える高い投稿数を記録している．
4位以降は徐々にコメント数が減少し，ユーザ10位で約1,400件，ユーザ20位で約800件，ユーザ30位で約600件となっている．
上位ユーザ間でのコメント数の減少は比較的滑らかであるが，10位以降で急激な減少がみられた．
公式アカウントを除く一般ユーザの中でも，上位層は1,000件以上の投稿を行っており，非常に高い活動性を示している．

\subsubsection{活動期間とコメント数の分布}
\figref{fig:activity_period_comment_heatmap}に示すように，横軸の活動期間（日数）と縦軸のメッセージ数の関係をヒートマップで可視化した結果である．
活動期間とは，ユーザが最初にコメントを投稿した日から最後にコメントを投稿した日までの日数を指す．
総ユーザ数35,228人のうち14,952名（42.4\%）のユーザが1コメントのみで，コメントを一度投稿してからそれ以降の投稿がないユーザであった．
そのため，ヒートマップでは除外されている．
コメント数が10〜50件の範囲では，活動期間が1日から300日まで幅広く分布している．
そのかわり，活動期間が0.2日あたりでコメント数が増加するにつれてユーザ密度が減少していることがわかる．
その上で活動期間が0.1日，つまり数時間の間に約300件のコメントを投稿する非常に高頻度なユーザも存在していることがわかる．
また，活動期間が100日を超えるユーザは縦軸全体に分散しており，長期間にわたってライブ配信を確認し，断続的にコメントを投稿している投稿頻度の低いユーザ（右下）が多いが，それと同時に高頻度で投稿するユーザ（右上）も一定数存在していることがわかる．
最も濃い赤色（約160以上のユーザ密度）は活動期間30〜200日かつメッセージ数2〜4件の領域に集中しており，長期間の活動期間を持つが投稿頻度はかなり低いユーザ層が顕著である．

\subsection{地震発生からコメントピークまでの時間分析}
地震発生時のコメント反応を分析するため，震度別にコメント数のピーク到達時間を分析した結果を\figref{fig:comment_peak}に示す．

\input{figure/comment_peak}

\figref{fig:comment_peak}に示すように，最大震度1（n=221），最大震度1（n=115），最大震度3（n=49），最大震度4（n=12），最大震度5弱（n=1）の5つのカテゴリに分類し，それぞれのピークまでの時間（秒）を箱ひげ図で表示している．

最大震度1と2のカテゴリでは，上方に多数の外れ値が存在する．
つまり，震度が小さい地震ほど，コメント数のピーク到達時間がばらつきやすいことを示している．

最大震度3のカテゴリでは，最小値が47.0秒，平均値が286.2秒，中央値が147.0秒である．箱の範囲は約100秒から約350秒であり，外れ値は約1,300秒から約3,300秒まで分布している．中央値が平均値よりも大幅に低く，外れ値の影響で平均値が引き上げられている．

最大震度4のカテゴリでは，最小値が83.0秒，平均値が230.2秒，中央値が232.5秒である．箱の範囲は約200秒から約450秒であり，最大震度1や最大震度2と比較してやや高い値を示している．

最大震度5弱のカテゴリでは，サンプル数が1件のみであるため，箱ひげ図は単一の線として表示されており，最小値が434.0秒，平均値が434.0秒，中央値が434.0秒である．

全体として，震度が増加するにつれて箱の位置がわずかに上方にシフトする傾向が見られる．しかし，最大震度1や2では外れ値もおおく，最大震度1では地震発生時刻から0秒に近い時間でピークが検出されケースも見られ信用度が低い．

% \begin{itemize}
%     \item \textbf{時間帯別}: 深夜2〜5時が最も少なく，夕方18〜22時にピーク
%     \item \textbf{曜日別}: 平日・休日で顕著な差は見られず
%     \item \textbf{地震発生との相関}: 震度4以上の地震発生後15分以内にコメント数が平常時の5〜10倍に急増
% \end{itemize}

% \subsection{ユーザ特性分析}

% \subsubsection{ユーザごとのコメント数分布}

% \begin{itemize}
%     \item \textbf{1回のみ投稿}: 全ユーザの約XX\%
%     \item \textbf{リピーター（10回以上）}: 約XX\%
%     \item \textbf{ヘビーユーザ（100回以上）}: 約X\%が全コメントの約XX\%を占める
% \end{itemize}

% この分布から，一部のアクティブユーザと多数の一見ユーザが混在する構造が明らかになった．

% \subsubsection{新規ユーザとリターン率}

% 地震発生時には新規ユーザが急増し，リターン率は以下の通りであった．

% \begin{itemize}
%     \item \textbf{震度4以上の地震後}: 新規ユーザが平常時の約X倍
%     \item \textbf{1週間以内のリターン率}: 約XX\%
%     \item \textbf{1ヶ月以内のリターン率}: 約XX\%
% \end{itemize}

% \section{地震発生時のコメント反応分析}

% \subsection{コメントピークと立ち上がり時間}

% 震度別にコメント数のピーク到達時間を分析した結果，以下の傾向が確認された．

% \begin{itemize}
%     \item \textbf{震度3〜4}: 地震発生後平均XX分でピーク到達
%     \item \textbf{震度5弱以上}: 地震発生後平均XX分でピーク到達
%     \item \textbf{外れ値}: 震度が小さいほど箱ひげ図の外れ値が多く，反応が不明瞭
% \end{itemize}

% 大きな地震ほど反応が明確で，ピーク時間も遅延する傾向が見られた．

% \subsection{感情分析結果の時系列変化}

% 地震発生前後の感情分析結果の推移を分析した結果，以下が確認された．

% \begin{itemize}
%     \item \textbf{発生直後（0〜5分）}: 「驚き」「恐れ」が急増
%     \item \textbf{5〜15分後}: ネガティブ感情がピークに達し，「怒り」「嫌悪」も増加
%     \item \textbf{30分以降}: 徐々に平常時の感情分布に回帰
% \end{itemize}

% \subsection{LLMを利用した災害関連コメント分析}

% （※この節は今後の実装予定として残す，または既に実施済みなら結果を記載）

% \begin{itemize}
%     \item \textbf{災害関連コメントのフィルタリング}: LLM（Gemma3等）を用いたゼロショット分類
%     \item \textbf{コメント内容のクラスタリング}: MiniLM + UMAPによる全期間のコメント類型化
% \end{itemize}